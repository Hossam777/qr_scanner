export default interface Guest {
    studentName: string | null,
    studentEmail: string | null,
    studentUniNumber: string | null,
    studentCollageAr: string | null,
    studentCollage: string | null,
    scanCategory: string | null,
    actualScan: string | null,
    maxScan: string | null,
}